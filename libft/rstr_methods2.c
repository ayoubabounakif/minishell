#include "libft.h"

/* int	rstr_lookup(t_rstr haystack, char *needle)
{
	int	i;
	int j;

	i = 0;
	j = 0;
	while (i < haystack->len)
	{
		while (j < haystack->len && j <  needle[j])
	}
	return (0);
} */